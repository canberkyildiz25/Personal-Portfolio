[build]
  command = "npm run build"
  publish = "dist"

[build.environment]
  # Vite 7 requires Node 20.19+ or 22.12+. Pinned so the build does not
  # depend on whatever default the Netlify UI happens to be set to.
  NODE_VERSION = "22"
